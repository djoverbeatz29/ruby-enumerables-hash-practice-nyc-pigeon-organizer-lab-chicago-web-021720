def nyc_pigeon_organizer(data)
  # write your code here!
  
result = {}
  
data.each{ |k1, v1|
  v1.each{ |k2, v2|
    v2.each{ |item|
      (result[item][k1]) ? 
    }
  }
}

for x in cf do
  for y in x do
    for z in y do
      for k in z.keys do
        if result[k]
  
end